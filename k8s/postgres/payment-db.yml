apiVersion: apps/v1
kind: Deployment

metadata:
  name: payment-db
  namespace: intelligent-supply-chain

spec:
  replicas: 1

  selector:
    matchLabels:
      app: payment-db

  template:

    metadata:
      labels:
        app: payment-db

    spec:

      containers:

        - name: payment-db

          image: postgres:16

          ports:
            - containerPort: 5432

          env:

            - name: POSTGRES_DB
              value: supply_paymentdb

            - name: POSTGRES_USER
              value: postgres

            - name: POSTGRES_PASSWORD
              value: postgres

---
apiVersion: v1
kind: Service

metadata:
  name: payment-db
  namespace: intelligent-supply-chain

spec:

  selector:
    app: payment-db

  ports:
    - port: 5432
      targetPort: 5432

  type: ClusterIP